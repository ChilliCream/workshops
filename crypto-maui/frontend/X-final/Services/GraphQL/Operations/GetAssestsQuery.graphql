query GetAssestsQuery($after: String)
{
  assets(first:2 after: $after) {
    nodes
    {
      id,
      imageUrl,
      isInWatchlist,
      symbol,
      name,
      slug,
      description,
      color,
      website,
      whitePaper,
      price {
        lastPrice
        change24Hour
      }
    }
    pageInfo {
        hasNextPage
        endCursor
    }
  }
}